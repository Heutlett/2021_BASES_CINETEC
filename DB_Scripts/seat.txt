CREATE TABLE SEAT
(
	room_id 			INT NOT NULL,
    number 				INT NOT NULL,
	status		 		VARCHAR(15) NOT NULL,
	PRIMARY KEY(room_id),
	UNIQUE(number)
);

ALTER TABLE SEAT
ADD CONSTRAINT SEAT_ROOM_FK FOREIGN KEY(room_id)
REFERENCES ROOM(id)

